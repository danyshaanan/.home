
These are my user configurations files.

You'd usually find them in this kind of configuration:

~/.home/.git/
~/.home/.bashrc
~/.home/.inputrc
~/.bashrc -> .home/.bashrc
~/.inputrc -> .home/.inputrc

This way I can easily manage these config files with git between several machines.

