##apt-get:
sudo apt-get install vim git curl							#cli essentials
sudo apt-get install chromium-browser build-essential ubuntu-restricted-extras		#gui essentials
sudo apt-get install tree lm-sensors hddtemp aptitude unrar screen fdupes trash-cli	#cli system
sudo apt-get install imagemagick jp2a renrot exif					#cli graphics
sudo apt-get install streamer ffmpeg sox mencoder mplayer vlc youtube-dl		#cli audio video
sudo apt-get install ddrescue testdisk scalpel						#cli recovery
sudo apt-get install gparted meld gimp gparted						#gui
sudo apt-get install mysql-server
sudo apt-get install gnome-session-fallback

sudo apt-get update && sudo apt-get upgrade && sudo apt-get dist-upgrade

##rsync:
#two lines to backup and create a hard-link-based snapshot:
time rsync -Phavy --delete-after src/ dest/ --dry-run
time rsync -Phavy --delete-after src/ dest-`date +"%y%m%d_%H%M%S"`/ --link-dest=../dest --dry-run
#this can be done more elegantly by replacing the `date` command with the `d` and `t` aliases from .bash_aliases

##streamer:
#Take a few camera shots over a second from /dev/video0:
streamer -c /dev/video0 -t 9 -r 9 -o ~/.webcam/webcam_`date +"\%Y\%m\%d_\%H\%M\%S"`-00.jpeg
#since at first the camera is not adjusted, the first pictures will be darker.

##sensors:
echo `d` `t` `sensors|grep -oe '+\([0-9]*.[0-9]\).C '`
##hddtemp:
sudo hddtemp /dev/sd?
##fdupes:
fdupes -rS . >> fdupes.txt && grep -e "[0-9]\{7,\} byte" -A 3 fdupes.txt

##imagemagick:
#Take a screenshot:
import -display :0.0 -window root ~/imagemagic_screen_`date +"%Y%m%d_%H%M%S"`.png
#change filetype / rename to sequance: (good for ffmpeg)
convert *.png eggscan-%03d.jpg
#average image:
convert -average *png average.png
#clean sketches:
mogrify -resize 10% -level 0%,80%,1 -rotate 180 -negate -alpha Copy -negate wawawawa.png
#combine images:
convert c1x*.jpg +append l1.jpg	#horizontally
convert c2x*.jpg +append l2.jpg	#horizontally
convert l*.jpg   -append e.jpg	#vertically
#create animated gif:
convert -delay 33 -loop 0 source_*.jpg result.gif #keep source files numbers consecutive

##ffmpeg:
#convert avi to mpg:
ffmpeg -i video.avi -sameq video.mpg
#concatenate two or move videos to one:
cat vid1.mpg vid2.mpg | ffmpeg -i - -sameq result.mpg
#create a vid from a sequence of files:
ffmpeg -r 24 -b 3600 -i %04d.jpg test3600.mp4
#convert an mp3 for ios:
ffmpeg -i file.mp3 file.m4a && mv file.m4a file.m4r
#cut a part of a video:
ffmpeg -ss 60 -t 5 -i source.mp4 -sameq source_60_to_65.mp4
#convert wav to mp3:
ffmpeg -i output.wav -sameq output.mp3
ffmpeg -i elinoreversed.wav -acodec libmp3lame -ac 2 -ab 128k -ar 44100 elinoreversed.mp3

##sox:
#reverse a wav:
sox -V audio.wav reveresed.wav reverse

##apache2:
#restart apache:
sudo /etc/init.d/apache2 restart
service apache2 restart

##vlc:
#stream in the input of /dev/video0:
vlc v4l2:///dev/video0

##unrar:
#open password protected rar:
unrar e -pPASSWORD archive.rar

##rename:
rename 's/-([0-9])\./-0$1\./' *.png && ll

#restore deleted files:
##ddrescue: 
sudo dd_rescue /dev/hdc hdimage #or: dd if=/dev/hdc of=hdimage conv=noerror
##testdisk:
sudo photorec hdimage
##scalpel
sudo vi /etc/scalpel/scalpel.conf #set file types
mkdir output
sudo scalpel file -vo output

#rotate screen:
xrandr -o inverted
xrandr -o normal


##bash:
#sudo last command:
sudo !!
#replace once in last commange:
^foo^bar
#replace a few times in last command?
!!:gs/foo/bar
#empty file:
> file.txt
#chain:
(sleep 3 && ls  &) ; (sleep 2 && ll &)
#forkbomb:
:(){:|:&};:






