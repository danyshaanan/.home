##Installations for ubuntu:
#cli:
sudo apt-get install vim git curl
sudo apt-get install tree lm-sensors hddtemp aptitude unrar screen fdupes trash-cli mysql-server
sudo apt-get install imagemagick jp2a renrot exif
sudo apt-get install streamer ffmpeg sox mencoder mplayer youtube-dl
sudo apt-get install ddrescue testdisk scalpel catdoc
#gui:
sudo apt-get install chromium-browser build-essential ubuntu-restricted-extras gnome-session-fallback
sudo apt-get install gparted meld gimp gitk vlc
#update:
sudo apt-get update && sudo apt-get upgrade

##Installations for osx:
#cli:
brew install aircrack-ng mdbtools mtr ssh-copy-id sshfs unrar wget vim curl p7zip
brew install ddrescue duff fdupes rename scalpel testdisk tree fcrackzip
brew install exif exiftool ffmpeg imagemagick jp2a mplayer sox youtube-dl mpg123 vimeo-downloader
brew install node brainfuck ruby ack haskell-platform python
brew install cdparanoia lame abcde # see https://gist.github.com/danyshaanan/6494096 for abcde

##Nodejs:
npm install -g nsyrc
npm install -g cli-mandelbrot
npm install -g geoexif
npm install -g tcmount

##Python:
pip install eyeD3 eyeD3-pip
pip install PIL

##Web downloads:
run http://www.truecrypt.org/downloads